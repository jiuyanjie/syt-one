server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jiuyanjie/config-test.git
          search-paths:
          username: jiuyanjie
          password: Shuhao88
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka